# 🧮 Calculadora de Console em Java

Este projeto é uma aplicação de backend desenvolvida para fixar os fundamentos da linguagem Java e lógica de programação. O foco foi criar uma solução limpa, funcional e que roda diretamente no terminal.

## 🚀 Tecnologias Utilizadas
* **Java** (JDK 25)
* **IDE:** Visual Studio Code
* **Conceitos aplicados:**
  * Estruturas condicionais (`switch/case` e `if/else`)
  * Tratamento de fluxo e exceções (evitar divisão por zero)
  * Entrada e Saída de dados (`Scanner` e `System.out`)
  * Formatação condicional de números (`printf`)

## ⚙️ Funcionalidades
- [x] **Operações básicas:** Soma, Subtração, Multiplicação e Divisão.
- [x] **Tratamento de erros:** Impede que o programa quebre ao tentar dividir por zero.
- [x] **Formatação inteligente:** Exibe resultados inteiros sem vírgula (ex: `4` em vez de `4.0`) e decimais apenas quando necessário.
- [x] **Internacionalização:** Aceita entrada de dados no padrão americano (ponto) independente do idioma do sistema.

## 📦 Como baixar e rodar

É necessário ter o **Java (JDK)** instalado em sua máquina.

1. **Clone o repositório:**
   ```bash
   git clone [https://github.com/SEU-USUARIO/calculadora-java.git](https://github.com/SEU-USUARIO/calculadora-java.git)

2.  **Entre na pasta do projeto:**

    ```bash
    cd calculadora-java
    ```

3.  **Compile o código:**

    ```bash
    javac Calculadora.java
    ```

4.  **Execute o programa:**

    ```bash
    java Calculadora
    ```

## 🖥️ Exemplo de Uso

```
------CALCULADORA RÁPIDA------
Digite o primeiro número: 10
Digite a operação (+, -, *, /): /
Digite o segundo número: 2
Resultado: 5
```

## 🧠 Aprendizados

Neste projeto, pude colocar em prática os conceitos iniciais do meu **Roadmap de Backend**:

  - **Lógica de Decisão:** Como usar o `switch-case` para criar menus de opções claros.
  - **Tratamento de Exceções:** A importância de validar dados (como o divisor ser zero) antes de processar o cálculo.
  - **Manipulação de Saída:** Uso do `printf` para controlar a precisão decimal.

## 👩‍💻 Autora

Desenvolvido por **Ana Luiza Fernandes Novaes** durante meus estudos de Backend e Java.
